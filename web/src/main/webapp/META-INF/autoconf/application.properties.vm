jdbc.driverClassName=${jdbc.driverClassName}
jdbc.url=${jdbc.url}
jdbc.username=${jdbc.username}
jdbc.password=${jdbc.password}

# dubbo
dubbo.application.name=${dubbo.application.name}
zookeeper.address=${zookeeper.address}
dubbo.port=${dubbo.port}
dubbo.service.athena.version=${dubbo.service.athena.version}
# 消费方
dubbo.consume.stall.version=${dubbo.consume.stall.version}
dubbo.consume.insurance.version=${dubbo.consume.insurance.version}
dubbo.consume.vin.version=${dubbo.consume.vin.version}

# redis
redis.host=${redis.host}

# 项目环境(dev, test, stable, online)
# 主要用于控制vin码查询接口，只有online（生产环境）才会去调vin-server项目接口（力洋vin码接口），其他环境一律不准调用
project.environment=${project.environment}

# 力洋vin码接口
vin.server.host=${vin.server.host}

# 搜索host
search.host=${search.host}

# ======== 保险物料推荐相关配置 ========
# 机油类目id
oil.cat.id=${oil.cat.id}
# 机油滤清器类目id
oil.filter.cat.id=${oil.filter.cat.id}
# 淘汽云修品牌id
yun.xiu.brand.id=${yun.xiu.brand.id}
